@!@from __future__ import print_function

# Add static addresses "hosts/static/$ip=$names"
PREFIX = 'hosts/static/'
for key, value in list(configRegistry.items()):
	if key.startswith(PREFIX) and key != PREFIX:
		print(("%s\t%s" % (key.split('/')[-1], value)))
@!@
